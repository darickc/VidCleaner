import { Page, Placeholder } from "../components/Page";

export function WordsPage() {
  return (
    <Page title="Words & Profiles" subtitle="Categories, custom words, whitelist and profiles.">
      <Placeholder milestone="M5">
        The built-in word lists land with the matcher in M1; this editor follows in M5.
      </Placeholder>
    </Page>
  );
}
