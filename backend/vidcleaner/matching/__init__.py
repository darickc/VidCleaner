"""Word lists, regex compilation and token normalization.

Nothing in this package imports ffmpeg, torch or the database, so the whole
matching engine is unit-testable on a bare checkout. See PLAN.md §7.
"""
